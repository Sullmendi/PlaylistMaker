<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <stroke
        android:color="@color/shadow_grey"
        android:width="1dp"
        android:dashWidth="@dimen/distance_thirty"
        android:dashGap="@dimen/distance_thirty"/>

    <corners android:radius="@dimen/distance_eight"/>
</shape>